{
  "name": "@nestbrain/cli",
  "version": "1.12.2",
  "private": true,
  "bin": {
    "nestbrain": "./dist/nestbrain.bundle.cjs"
  },
  "scripts": {
    "build": "tsc && pnpm bundle",
    "bundle": "node build/bundle.mjs",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@nestbrain/core": "workspace:*",
    "@nestbrain/shared": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
