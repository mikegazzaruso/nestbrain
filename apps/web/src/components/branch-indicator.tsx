"use client";

// Open-core stub — the real branch chip + source control entry point ships
// with the Dev module and replaces this file in official builds.

export function BranchIndicator() {
  return null;
}
